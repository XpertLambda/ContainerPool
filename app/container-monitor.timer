[Unit]
Description=Container Health Monitor Timer
Requires=container-monitor.service

[Timer]
# Run every 2 minutes
OnBootSec=1min
OnUnitActiveSec=0.5min

# Make sure we run even if the system was off
Persistent=true

[Install]
WantedBy=timers.target
